---

# Apple 级 UI/UX 审查报告：资料库页面及子页面

**审查范围**：`http://localhost:5173/#/library` 主页面 + `/#/courses/:id/library` 工作区子页面
**审查日期**：2026-08-14

---

## 一、文字系统

### P2 | 页面标题与副标题层级跳跃过大

- **位置**：[Library.jsx#L804-L806](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.jsx#L804-L806)
- **当前问题**：H1 标题 `clamp(24px, 3vw, 30px)` 紧接 13px 副标题，中间无任何过渡层级，视觉跳跃感强。
- **为什么是问题**：Apple HIG 中标题与正文之间通常有明确的层级阶梯（如 Title → Subtitle → Body），当前 24-30px 直接跳到 13px 缺乏中间层。
- **推荐修改**：考虑将副标题提升至 14px，或在标题下方增加一行 11px 的 eyebrow 标签建立层级过渡。

### P2 | 卡片标题使用非标准字重

- **位置**：[Library.css#L274](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L274)
- **当前问题**：`.library-card__title` 使用 `font-weight: 650`。
- **为什么是问题**：650 不是标准字重档位（标准为 400/500/600/700），不同字体渲染引擎可能产生不一致的视觉效果。
- **推荐修改**：改为 `font-weight: 600`。

### P2 | 卡片描述区域固定最小高度浪费空间

- **位置**：[Library.css#L291-L300](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L291-L300)
- **当前问题**：`.library-card__desc` 设置了 `min-height: 32px` 配合 `-webkit-line-clamp: 2`。当内容只有一行时（如"Markdown · 62 B"），仍占用两行高度。
- **为什么是问题**：固定最小高度在内容不足时产生不必要的空白，卡片纵向空间利用率低。
- **推荐修改**：移除 `min-height: 32px`，让描述区域高度跟随内容自适应。

### P3 | 模态框标题与页面标题字号不一致

- **位置**：[Library.css#L421-L428](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L421-L428)
- **当前问题**：模态框标题 `font-size: 18px`，页面主标题 `clamp(24px, 3vw, 30px)`。模态框作为详情层，标题比主列表标题小 6-12px。
- **为什么是问题**：用户打开详情后，视觉焦点应自然落在标题上，18px 在暗色背景下略显单薄。
- **推荐修改**：模态框标题提升至 20px，与页面标题形成更自然的层级关系。

---

## 二、按钮

### P2 | 归档按钮（ghost）在深色背景下可发现性低

- **位置**：[Library.jsx#L748-L754](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.jsx#L748-L754)
- **当前问题**：模态框底部"归档"按钮使用 `.button--ghost` 样式（透明背景、`--text-secondary` 颜色），在 `#171717` 深色背景上几乎不可见。
- **为什么是问题**：归档是破坏性/重要操作，但视觉权重反而最低。
- **推荐修改**：至少使用 `.button--secondary` 样式，或增加特定 hover 时的危险色提示。

### P2 | 批量操作栏"取消"按钮视觉权重不足

- **位置**：[Library.jsx#L994](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.jsx#L994)
- **当前问题**：批量操作栏中"取消"按钮为 ghost 样式，在浮动的深色胶囊条中不够明显。
- **为什么是问题**：用户选中多个卡片后，取消操作是关键的退出路径，应有足够视觉存在感。
- **推荐修改**：改为 secondary 样式或至少提升颜色至 `--text-primary`。

---

## 三、图标

### P2 | 文件类型徽章使用纯文字而非图标

- **位置**：[Library.jsx#L938-L940](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.jsx#L938-L940)
- **当前问题**：卡片和模态框中的文件类型使用纯文字缩写（"MD"、"PDF"）代替图标。
- **为什么是问题**：28×34px 的矩形区域只放 9px 的文字，视觉内容丰富度低，辨识速度不如图标。
- **推荐修改**：保留文字缩写的同时，增加文件类型对应的 lucide 图标（如 `FileText`、`FileType`），将文字作为辅助 overlay。

### P3 | 知识圆点指示器过于细微

- **位置**：[Library.css#L660-L668](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L660-L668)
- **当前问题**：`.ws-knowledge-dot` 为 8×8px 空心圆环（border: 2px），在知识列表中的视觉存在感较弱。
- **为什么是问题**：选中态依赖左侧 2px 的 inset shadow，圆点本身对选中/未选中区分的贡献不大。
- **推荐修改**：选中态时圆点填充为实心（`background: var(--brand-primary)`），增强状态区分度。

---

## 四、输入控件

### P2 | 搜索框与下拉筛选器高度不一致

- **位置**：[Library.css#L53-L118](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L53-L118)
- **当前问题**：搜索框使用 `min-height: 36px`，全局标准 input/select 为 `min-height: 40px`。筛选器也使用 `min-height: 36px`。
- **为什么是问题**：工具栏中三个控件（搜索、筛选、排序）高度不统一，且与全局组件规范不一致，视觉节奏被打断。
- **推荐修改**：统一为 `min-height: 36px` 或全局标准 `40px`，保持一致。

### P3 | Select 下拉箭头使用浏览器默认样式

- **位置**：[Library.css#L59-L67](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L59-L67)
- **当前问题**：筛选和排序 select 元素未自定义下拉箭头，使用浏览器默认渲染。
- **为什么是问题**：在暗色主题下，浏览器默认箭头可能为深色，在深色背景上不可见或不协调。
- **推荐修改**：使用 `appearance: none` + 自定义箭头 SVG 背景。

---

## 五、间距与对齐

### P2 | 页面各处间距值缺乏统一尺度

- **位置**：多处
- **当前问题**：卡片网格 `gap: 14px`，页面堆叠 `gap: 20px`，工具栏 `gap: 10px`，卡片内部 `gap: 9px`，模态框滚动区 `gap: 14px`。这些值（9, 10, 14, 20）未形成清晰的倍数关系。
- **为什么是问题**：Apple 设计系统使用 4px 基准网格（4, 8, 12, 16, 20, 24...），当前 9px、14px 等值偏离这个体系，导致视觉节奏不一致。
- **推荐修改**：统一使用 4px 基准：9px → 8px 或 12px，14px → 12px 或 16px。

### P2 | 模态框内容区 padding 不对称

- **位置**：[Library.css#L462-L470](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L462-L470)
- **当前问题**：`.library-modal__scroll` 使用 `padding: 2px 22px 20px`。顶部 2px + 头部 16px 底部 padding = 18px，与底部 20px 不对称。
- **为什么是问题**：内容区顶部和底部的视觉留白不对称，影响视觉平衡。
- **推荐修改**：统一为 `padding: 0 22px 20px`，让头部 padding-bottom 负责顶部间距。

---

## 六、容器与视觉层级

### P1 | 模态框内多个 section 使用相同背景色

- **位置**：[Library.css#L473-L530](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L473-L530)
- **当前问题**：`.library-modal__banner`、`.ws-kpi-strip`、`.ws-card`、`.library-metadata-editor` 全部使用 `background: var(--surface-container)` (#2c2c2c)。在模态框背景 `#242424` 上，这些区块缺乏视觉区分。
- **为什么是问题**：用户难以快速区分"状态横幅"、"统计概览"、"资料信息"、"知识候选"等不同语义区块，所有卡片看起来都一样。
- **推荐修改**：为不同语义区块使用差异化背景色或至少不同的边框/阴影处理，如 KPI strip 使用更突出的背景色，metadata editor 使用更淡的背景。

### P1 | 主资料库页与工作区子页面布局不一致

- **位置**：主页面 vs `/#/courses/:id/library` 子页面
- **当前问题**：主 Library 页使用卡片网格布局，工作区子页面使用文件列表+右侧详情面板布局，两者视觉语言完全不同。
- **为什么是问题**：用户从主资料库切换到工作区资料库时，会感到进入了完全不同的页面，增加认知负担。
- **推荐修改**：统一两处的核心布局模式（至少卡片/列表行样式一致），或明确说明两处功能差异。

### P2 | 空状态使用虚线边框与整体风格不一致

- **位置**：[Library.css#L122-L135](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L122-L135)
- **当前问题**：`.library-empty-state` 使用 `border: 1.5px dashed var(--border-strong)`，而页面其他所有容器都使用实线边框。
- **为什么是问题**：虚线边框在暗色主题中视觉噪音较大，且与整体实线边框语言不一致。
- **推荐修改**：改为实线边框 + 更淡的颜色，或使用纯背景色区块 + 居中内容表达空状态。

---

## 八、Dialog / Modal / Sheet

### P2 | 模态框背景点击关闭无视觉提示

- **位置**：[Library.jsx#L652-L654](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.jsx#L652-L654)
- **当前问题**：点击模态框外部遮罩可关闭（`onPointerDown`），但没有 cursor 变化或任何视觉提示。
- **为什么是问题**：用户不知道可以点击外部关闭，降低了操作效率。
- **推荐修改**：至少在外层遮罩添加 `cursor: pointer`（移动端除外）。

### P2 | 模态框内容过长时缺少底部渐变遮罩

- **位置**：[Library.css#L462-L470](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L462-L470)
- **当前问题**：`.library-modal__scroll` 处理滚动，但滚动区域底部直接与 footer 相接，没有 fade-out 渐变提示。
- **为什么是问题**：当内容较长时，用户不知道还有内容可滚动，底部内容被 footer 突然截断。
- **推荐修改**：在滚动区域底部添加 `linear-gradient(transparent, var(--surface-muted))` 的遮罩，高度约 20px。

---

## 九、滚动与浮层

### P2 | 批量操作栏可能遮挡最后一行卡片

- **位置**：[Library.css#L942-L958](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L942-L958)
- **当前问题**：`.library-batch-bar` 使用 `position: sticky; bottom: 16px`，当卡片数量少时（如 1 张），浮层紧贴最后一张卡片。
- **为什么是问题**：在只有 1-2 张卡片的场景下，浮层遮挡了卡片内容，用户需要先取消选择才能看到被挡信息。
- **推荐修改**：增加 `bottom` 值或仅在卡片数量 ≥ 3 时显示批量栏，或增加页面底部 padding 为浮层留出空间。

---

## 十、状态反馈

### P2 | 卡片复选框仅 hover 时可见

- **位置**：[Library.css#L324-L345](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L324-L345)
- **当前问题**：`.library-card__check` 默认 `opacity: 0`，仅在 hover/focus-within/batch-active 时显示。
- **为什么是问题**：用户无法在不移动鼠标的情况下发现批量选择功能，触摸设备上完全不可用。
- **推荐修改**：移动端始终显示复选框；桌面端可保持 hover 显示，但需确认这不是关键功能入口。

### P3 | 无匹配结果状态提示不够友好

- **位置**：[Library.jsx#L882-L889](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.jsx#L882-L889)
- **当前问题**：无匹配时显示搜索图标 + "没有找到匹配的资料" + "清除筛选" 按钮。
- **为什么是问题**：缺少关于当前筛选条件的具体说明（如"当前筛选：状态=处理失败，关键词=xxx"），用户不清楚为什么没有结果。
- **推荐修改**：增加当前筛选条件的文字说明，帮助用户理解无结果的原因。

---

## 十二、一致性

### P2 | 状态 Pill 样式定义分散

- **位置**：[global.css#L276-L310](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/styles/global.css#L276-L310) vs [Library.css#L872-L921](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L872-L921)
- **当前问题**：全局定义了 `.status-pill--success/danger/warning/neutral`，Library.css 又定义了 `.status-pill--document-completed/pending/failed` 等变体。两者语义重叠但样式略有不同。
- **为什么是问题**：维护两套 pill 样式容易导致不一致，且新开发者不清楚何时使用哪个。
- **推荐修改**：将 document-specific pill 样式合并到全局，或统一通过 tone 参数生成。

---

## 十三、可访问性

### P2 | 知识列表项缺少键盘导航提示

- **位置**：[Library.jsx#L588-L607](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.jsx#L588-L607)
- **当前问题**：知识列表使用 `<ul><li><button>` 结构，但未设置 `role="listbox"` / `role="option"` 或实现箭头键导航。
- **为什么是问题**：键盘用户只能通过 Tab 逐个遍历列表项，无法使用箭头键快速切换。
- **推荐修改**：添加 `role="listbox"` 到 `<ul>`，`role="option"` 到 `<li>`，并实现箭头键导航。

### P3 | 搜索框缺少清除按钮

- **位置**：[Library.jsx#L851-L862](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.jsx#L851-L862)
- **当前问题**：搜索框输入后没有一键清除按钮，用户需要手动删除文字或重新提交空搜索。
- **为什么是问题**：降低操作效率，尤其是移动端。
- **推荐修改**：当 `queryDraft` 非空时，在搜索框右侧显示 X 清除按钮。

---

## 十四、macOS / Apple 体验

### P2 | 卡片网格缺乏弹性布局感

- **位置**：[Library.css#L186-L193](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L186-L193)
- **当前问题**：`grid-template-columns: repeat(auto-fill, minmax(270px, 1fr))` 在宽屏下会产生过多列，卡片横向拉伸过大。
- **为什么是问题**：Apple 设计偏好适中的内容宽度，过宽的卡片降低可读性和视觉精致感。
- **推荐修改**：增加 `max-width` 限制或使用 `auto-fit` + 合理的最大列数。

### P3 | 模态框缺少弹性动画曲线

- **位置**：[Library.css#L389-L398](file:///Users/xike/Documents/Docs/Askora/apps/frontend/src/pages/Library.css#L389-L398)
- **当前问题**：模态框动画使用 `ease-out`，而 Apple 系统动画使用弹性曲线（spring-like）。
- **为什么是问题**：`ease-out` 动画在 macOS 环境下显得生硬，缺乏 Apple 特有的流畅感。
- **推荐修改**：使用 `cubic-bezier(0.16, 1, 0.3, 1)`（类似 iOS spring 曲线）替代 `ease-out`。

---

## 全局一致性检查

| 维度 | 评估 |
|------|------|
| Typography 层级 | 基本统一，但标题与正文间跳跃偏大 (P2) |
| Spacing 规则 | 未使用 4px 基准网格，多处值不一致 (P2) |
| Button/Input 组件统一性 | 高度不一致（36px vs 40px），pill 样式定义分散 (P2) |
| 状态完整性 | 各状态（loading/empty/error/success）均有覆盖，但 checkbox 可发现性差 (P2) |
| Popover/Modal 定位 | 模态框实现良好，但缺少滚动渐变提示 (P2) |
| 响应式布局 | 移动端适配完整，全屏模态框处理得当 |
| 视觉噪音 | 信息密度适中，但多个相同背景色区块缺乏区分 (P1) |
| 极简/克制程度 | 整体克制，但虚线边框空状态和批量浮层略显突兀 |

---

## 总结

**P0 问题**：0 个
**P1 问题**：2 个（模态框区块视觉区分不足、主/子页面布局不一致）
**P2 问题**：18 个（涵盖文字、间距、按钮、输入控件、容器、状态、可访问性等）
**P3 问题**：6 个（细节优化）

**整体评价**：页面结构和交互逻辑扎实，状态覆盖完整，实现了反暗黑模式的设计（如实说明"不会用装饰性连线冒充先修关系"）。主要问题集中在视觉层级的细节打磨和一致性收敛上，属于可快速修复的 P2 级问题。建议优先处理两个 P1 问题：模态框内区块的视觉区分，以及主/子页面布局一致性。