---

# Apple 级 UI/UX 审查报告：资料库（Library）及子页面

## 1. 资料库主页面（Library Grid）

### 1.1 卡片网格布局

**问题位置** → `.library-grid` | `grid-template-columns: repeat(auto-fill, minmax(270px, 1fr))`，gap: 14px

当前问题：网格列宽从 270px 开始自适应，当窗口宽度为 900px 附近时可能出现最后一行只有 1 张卡片，中间大段空白。

严重程度：**P2**

推荐修改：考虑使用 `minmax(280px, 1fr)` 或配合 `grid-auto-rows` 让卡片高度统一，避免单张卡片被拉伸。

---

### 1.2 字体系统混乱

**问题位置** → 全页面多处使用了非标准字号

当前问题：
- `.library-card__title`：`13.5px`（非常见系统字号）
- `.library-card__match`：`11.5px`
- `.library-card__badge`：`9px`
- `.library-card__meta .status-pill`：`10.5px`
- `.library-toolbar__filter / __sort`：`12.5px`
- `.library-card__date`：`11px`

这些字号在 Apple 字体层级中没有对应关系，9px 在桌面端几乎不可读，`13.5px` 也不是标准字号。

严重程度：**P2**

推荐修改：统一为 Apple 标准字号层级：`13px`（body）/ `12px`（caption）/ `11px`（small）/ `15px`（subhead）。`9px` 的 badge 应至少提升到 `10px` 或直接移除，用文本代替。

---

### 1.3 复选框默认隐藏（Hover-only 发现）

**问题位置** → `.library-card__check` | `opacity: 0`，仅 `:hover` / `:focus-within` / `[data-batch-active]` 时显示

当前问题：批量操作复选框默认完全隐藏，用户必须悬停或已选中至少一张卡片后才能看到。这引入了"先发现秘密功能"的认知负担。若用户不知道可以批量操作，就永远不会触发批量模式。

严重程度：**P1**

推荐修改：默认显示半透明复选框（`opacity: 0.35`），hover 或选中时变为 `opacity: 1`。这样用户始终知道批量操作的存在。

---

### 1.4 搜索输入框与原生 Select 不统一

**问题位置** → `.library-toolbar__search` 中的搜索输入框 vs `.library-toolbar__filter` 和 `__sort` 原生 `<select>`

当前问题：搜索输入框用 `input-with-icon` 自定义样式，有 `hover`→背景变化、`focus-visible`→品牌色边框+阴影。而筛选和排序下拉框是原生 `<select>`，尽管有 `border-radius: 9px` 和 `hover` 效果，但浏览器下拉面板（popup）完全不受控，在 macOS 上会显示原生系统样式，与整体暗色主题割裂。

严重程度：**P2**

推荐修改：用自定义 Select 组件替换原生 `<select>`，确保下拉面板跟随暗色主题和品牌色。

---

### 1.5 空状态与筛选无结果状态

**问题位置** → `.library-empty-state`（无资料）和 `.library-no-match`（筛选无结果）

当前问题：两者都合理，但 `.library-no-match` 缺少"建议上传资料"的引导路径。用户看到"没有找到匹配的资料"后，唯一操作是"清除筛选"，没有引导去上传。

严重程度：**P3**

推荐修改：在"没有匹配"状态中增加"或导入新资料"的链接，保持用户工作流畅通。

---

### 1.6 卡片选中状态不明确

**问题位置** → `.library-card.is-open` | `box-shadow: 0 0 0 1px color-mix(in srgb, var(--brand-primary) 40%, transparent)`

当前问题：选中态（打开弹窗时）的 border 是品牌色混合 55% + 1px 阴影，区分度很低。在 `--surface-muted: #242424` 背景上，`#2c2c2c` 的 border 与 `#242424` 的卡片背景差别极小。

严重程度：**P2**

推荐修改：选中态使用更明显的品牌色边框（`border-color: var(--brand-primary)`），或增加 `inset 2px 0 0 var(--brand-primary)` 的左侧指示条。

---

### 1.7 分页器在卡片少时偏移

**问题位置** → `.pagination` | `display: flex; justify-content: center`

当前问题：分页器一直存在（当 `totalPages > 1`），但卡片数量少时（如仅 2 页共 21 条），分页器浮在底部，与卡片之间间距不稳定。

严重程度：**P3**

推荐修改：确保分页器与底部保持固定间距，或使用 `margin-top: auto` 结合 `page-stack` 的 flex 属性。

---

## 2. 详情弹窗（Library Detail Modal）

### 2.1 顶部文件类型 Badge 尺寸过大

**问题位置** → `.library-modal__badge` | `width: 44px; height: 52px; font-size: 10.5px`

当前问题：Modal 中的 badge 比卡片中的 badge 大很多（卡片中 28×34px），但字号仅从 9px 变为 10.5px，视觉上是一个大区域配小字，比例失衡。在 Apple 设计中，这种"大容器+小内容"是减分项。

严重程度：**P2**

推荐修改：缩小 badge 尺寸至 36×44px 或 32×40px，提高字号至 12px，或考虑使用更轻量的文件图标替代文字 badge。

---

### 2.2 状态横幅（Banner）信息密度高但视觉层级不清

**问题位置** → `.library-modal__banner` | 包含两个 status-pill 加一段描述文字

当前问题：banner 内同时展示"处理状态"和"学习状态"两个 pill 再加一段说明文字。三个元素在视觉上缺乏优先级区分——两个 pill 尺寸相同，文字在 flex-wrap 后独占一行。用户难以一眼区分"当前状态"和"其他状态"。

严重程度：**P2**

推荐修改：将两个 pill 合并，仅展示"处理状态"（因为学习状态在卡片上已有），或使用一个主 pill 加一个辅助标签（`<small>` 形式）。

---

### 2.3 KPI 数字过大

**问题位置** → `.ws-kpi-block__num` | `font-size: 26px`

当前问题：在弹窗内容区中 "26px" 的数字只有"个知识候选"和"条关系"两个指标，但数字占比过大，信息密度低。在 Apple 内容层级中，这是一种"过度强调不重要数据"的问题。

严重程度：**P3**

推荐修改：缩小至 `18px` 或 `20px`，与旁边的"个知识候选"标签（12px）形成更平衡的比例。

---

### 2.4 资料信息列表的 dt 固定宽度可能导致溢出

**问题位置** → `.ws-meta-row dt` | `width: 52px`

当前问题：`dt` 固定 52px 宽度，但内容如"导入时间"（4 个字）占约 52px 刚好。如果未来增加更长的字段标签（如"文件大小"已占 4 个字），可能导致文字换行或排版混乱。

严重程度：**P3**

推荐修改：用 `min-width` 代替固定 `width`，或使用 `flex: 0 0 auto` 结合 `white-space: nowrap`。

---

### 2.5 编辑资料信息（details/summary）样式不一致

**问题位置** → `.library-metadata-editor` | 使用原生 `<details>` / `<summary>` 元素

当前问题：这是一个 native disclosure widget，尽管自定义了箭头和 hover，但它的展开/折叠动画、键盘交互行为与弹窗内其他交互元素（按钮、链接）完全不同。特别是在 macOS 上，`<details>` 的展开是瞬间的，没有平滑动画，与 modal 的 `fade-in` / `scale-in` 动画形成对比。

严重程度：**P2**

推荐修改：使用自定义折叠组件（`useState` 控制），实现平滑高度过渡动画，与整体交互模型一致。

---

### 2.6 弹窗内按钮区域溢出

**问题位置** → `.library-modal__footer` | `display: flex; justify-content: space-between`

当前问题：footer 左侧是"归档"按钮，右侧是一个或多个功能按钮。当"使用新版策略重新检查"+"打开原文"+"开始学习"三个按钮同时出现时，在较短窗口宽度下会溢出。

严重程度：**P2**

推荐修改：在 `gap` 上使用 `flex-wrap: wrap` 并允许按钮换行，或在小窗口下将次要按钮收进 More 菜单。

---

### 2.7 "打开原文"链接不经过前端路由

**问题位置** → `Library.jsx:770` | `href="/documents/${encodeURIComponent(selectedDocument.document_id)}/content"`

当前问题：这是一个直接指向后端的 `<a>` 链接，`target="_blank"` 打开。但 Vite proxy 不代理 `/documents` 路径，后端也没有 `/documents/{id}/content` 路由。这会导致 404 或打开空白页。

严重程度：**P1**

推荐修改：确认后端有对应路由，或在 Vite config 中为 `/documents` 添加代理规则。否则应隐藏该按钮或显示 "暂不可用" 状态。

---

## 3. 上传后去向选择（MaterialDestination）

### 3.1 背景遮罩颜色不一致

**问题位置** → `.material-destination` | `background: color-mix(in srgb, var(--text-primary) 32%, transparent)`

当前问题：这个遮罩使用 `--text-primary`（`#e1e1e1`）混合 32%，产生的是浅灰色遮罩。而 Library modal 使用 `rgba(10, 10, 10, 0.62)` 暗色遮罩。两个弹窗的遮罩层风格不一致。

严重程度：**P2**

推荐修改：统一使用与 Library modal 相同的遮罩样式（`rgba(10, 10, 10, 0.62)` + `backdrop-filter: blur(3px)`）。

---

### 3.2 使用不同的 Button 组件体系

**问题位置** → `MaterialDestination.jsx` 使用 `Button` 组件 → `ds-btn` 类，而 Library 页面使用 `button button--primary` / `button--secondary` 类

当前问题：`ds-btn` 的尺寸为 `height: 32px; padding: 0 16px; font-size: 13px`。而 Library 页面的按钮尺寸为 `min-height: 40px; padding: 9px 14px`。二者尺寸不同，视觉上不一致。

严重程度：**P1**

推荐修改：统一使用同一套按钮体系。要么全部用 `ds-btn`，要么全部用 `.button` 类。

---

### 3.3 原生表单控件无样式

**问题位置** → `MaterialDestination` 中的 `<select>` 和 `<input>` 元素

当前问题：这些表单控件是原生浏览器样式，没有暗色主题适配。在 macOS 上，`<select>` 的弹出菜单是系统亮色样式，与暗色 UI 割裂。

严重程度：**P2**

推荐修改：使用自定义 Select 组件，至少设置 `appearance: none` + 自定义样式。

---

## 4. 开始学习子页面（BookLearningLaunch）

### 4.1 按钮体系再次混用

**问题位置** → `BookLearningLaunch.jsx` 中既有 `button button--primary` 又有 `button--prominent`，还有 `button button--ghost`

当前问题：`button--prominent` 是自定义的 `min-height: 44px`，而 `button--primary` 是 `min-height: 40px`。同一页面中同时存在两种高度（40px 和 44px）的 Primary button，缺乏一致性。

严重程度：**P2**

推荐修改：统一 Primary button 高度，或为"开始学习"等关键 CTA 统一使用 `--prominent` 变体并保持一致。

---

### 4.2 状态进度条（Learning Progress）在步骤少时视觉不完整

**问题位置** → `.learning-progress` | 3 个步骤 "准备→起点→本次学习"

当前问题：三个步骤的连线通过 `::before` 伪元素实现，从"右 50% 到左 50%"。但步骤 1 的 `::before` 被隐藏（`display: none`），步骤 2 和 3 的连线从中间开始/结束，导致视觉上连线不完整。

严重程度：**P3**

推荐修改：改进连线算法，使用 `flex` 布局 + 独立的连线元素，确保线段从步骤 1 右侧贯穿到步骤 3 左侧。

---

### 4.3 技术详情面板暴露原始实现细节

**问题位置** → `.learning-technical` | 展示 `state`、`mode`、`provider`、`model`、`prompt_version`、`reason_codes`、`owner_refs`

当前问题：虽然这是用 `<details>` 折叠的，但暴露了 `provider`、`model`、`prompt_version`、`reason_codes` 等原始实现细节。这不是用户应该看到的——既没有解释价值，也容易让用户困惑。

严重程度：**P2**

推荐修改：仅保留 `state` 这个用户可理解的调试信息，或完全移除该面板。如果保留，应添加用户友好的标签解释。

---

### 4.4 教学对话中的 RichMessage 懒加载

**问题位置** → `BookLearningLaunch.jsx:429` | `<Suspense fallback={<p>{turn.reply_text}</p>}><RichMessage ... /></Suspense>`

当前问题：`RichMessage` 使用 `lazy()` 加载，fallback 是纯文本。如果网速慢或首次加载，用户会先看到纯文本，然后突然变成富文本渲染，产生"闪烁"效果。

严重程度：**P3**

推荐修改：预加载 `RichMessage` 组件（不在 `lazy` 中），或使用 `startTransition` 确保渲染不阻塞交互。

---

### 4.5 多选诊断题的 radio 样式未自定义

**问题位置** → `BookLearningLaunch.jsx:480` | `<fieldset><legend>...</legend>{item.options.map(...)}</fieldset>`

当前问题：`<input type="radio">` 使用原生样式，绿色主题下不匹配。此外，`fieldset` 的 `border: 1px solid var(--border)` 在暗色主题下几乎不可见。

严重程度：**P3**

推荐修改：自定义 radio 样式（`appearance: none` + 自定义勾选圆圈），或使用按钮组样式的选择器替代 radio。

---

## 5. "打开原文"子页面（`/documents/:id/content`）

### 5.1 路由不存在

**问题位置** → `Library.jsx:770` 的链接 + `vite.config.js` 无 `/documents` 代理

当前问题：这是一个直接指向后端的链接，但 Vite proxy 只代理 `/api` 和 `/health`，不代理 `/documents`。后端也没有匹配的路由。点击后几乎必然 404。

严重程度：**P0**

推荐修改：在 Vite config 中添加 `/documents` 代理规则，或确认后端有对应路由，或在缺少路由时隐藏该按钮。

---

## 6. 全局一致性检查

### 6.1 按钮体系不统一

Library 页面使用 `.button` 类系（`min-height: 40px`），MaterialDestination 使用 `ds-btn` 类系（`height: 32px`），BookLearningLaunch 混用两者。同一个产品中不应出现两种按钮尺寸。

**P1**

### 6.2 字号层级无统一规则

页面中出现了 `9px`、`10.5px`、`11px`、`11.5px`、`12px`、`12.5px`、`13px`、`13.5px`、`14.5px`、`15px`、`17px`、`18px`、`19px`、`26px` 等大量非标准字号。Apple 设计典型字号层级用 11/12/13/15/17/20/24/28/34 等标准值。当前字号体系缺乏约束。

**P2**

### 6.3 弹窗遮罩层不统一

Library modal 使用 `rgba(10, 10, 10, 0.62) + backdrop-filter: blur(3px)`，MaterialDestination 使用 `color-mix(in srgb, var(--text-primary) 32%, transparent)`（浅灰色遮罩，无 blur）。

**P2**

### 6.4 离散集合与标签没有 UI 组件

`availableTags` 和 `availableCollections` 在批量操作栏中通过原生 `<select>` 展示，没有可视化标签/集合的 Tags UI。用户无法直观看到已有标签是什么。

**P2**

### 6.5 归档视图（Archived）没有独立入口

Library 支持 `archived` 参数，但页面上没有"归档"视图切换。用户只能通过"已归档"状态筛选或批量操作提示语来间接感知归档功能的存在。

**P2**

### 6.6 响应式断点覆盖率不足

Library 有 `@media (max-width: 768px)` 和 `(max-width: 1100px)` 两个断点，但在 1100px～1300px 的中等宽度下，Modal 宽度 `min(760px, 100%)` 在 800px 窗口下会完全占满，padding 压缩后体验不佳。

**P3**

---

## 严重程度总结

| 等级 | 数量 | 最高优先级 |
|------|------|-----------|
| **P0** | 1 | "打开原文"链接指向不存在的路由，点击后 404 |
| **P1** | 4 | 复选框默认隐藏、按钮体系不统一、详情弹窗链接 404、MaterialDestination 按钮体系不一致 |
| **P2** | 12 | 字号混乱、原生 Select 不统一、卡片选中态不明显、Badge 比例失衡、Banner 信息层级不清、details 组件样式不一致、弹窗 footer 溢出、遮罩层不统一、BookLearning 按钮混用、技术详情暴露实现细节、标签无 UI 组件、归档无入口 |
| **P3** | 5 | 分页器偏移、KPI 数字过大、dt 固定宽度、进度条连线不完整、RichMessage 懒加载闪烁 |

---

以上审查基于代码分析（因无法直接访问浏览器渲染）。建议优先修复 **P0 和 P1** 问题，这些直接影响功能可用性和整体一致性。