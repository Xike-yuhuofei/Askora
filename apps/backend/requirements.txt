# =============================================================================
# Askora Backend - 生产环境依赖
# =============================================================================
# 注意：本文件用于 Docker 构建缓存优化，实际安装以 pyproject.toml 为准。
# 安装方式：pip install -e .（读取 pyproject.toml 的 dependencies）
#
# Docker 构建时优先 COPY pyproject.toml 并 pip install -e .，
# 因此本文件列出的是关键运行时依赖，便于单独 Docker 层缓存。
# =============================================================================

# Web 框架
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# 数据库
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0

# 缓存
redis>=5.0.0

# 配置与校验
pydantic>=2.6.0
pydantic-settings>=2.2.0

# 认证与加密
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
cryptography>=42.0.0

# HTTP 客户端
httpx>=0.27.0

# 消息队列
aiokafka>=0.10.0

# 监控与追踪
prometheus-client>=0.20.0
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0

# 日志
structlog>=24.1.0

# 工具
tenacity>=8.2.0
python-multipart>=0.0.9
email-validator>=2.1.0
phonenumbers>=8.13.0

# 中文分词
jieba>=0.42.1