# Askora v0.3 — Adaptive Teaching Loop Completion Report

> Engineering Gate：PASS
> Policy Correctness Gate：PASS
> Learning Evidence Gate：LEARNING_EVIDENCE_INSUFFICIENT
> Baseline Status：ENGINEERING/POLICY VERTICAL SLICE FROZEN
> Freeze Date：2026-08-07
> Local completion commit：见 `feat(EXEC-013): complete v0.3 release gate` 提交

## 1. 结论

EXEC-007～013 已按依赖顺序完成。v0.3 Adaptive Teaching Loop 的工程正确性与策略正确性达到冻结条件；仓库没有满足 Spec 的真实 human learning efficacy experiment，因此学习证据状态必须保持：

```text
LEARNING_EVIDENCE_INSUFFICIENT
```

本报告不声称 adaptive policy 比 fixed strategy、legacy selector 或自由 LLM tutor 更有效。OPVE、G0/G1、synthetic learner 与真实模型连通性只构成工程/策略证据。

## 2. Candidate Commit / CI

| EXEC | Commit / evidence | Result |
|---|---|---|
| EXEC-007 | `c97e8ba` / `eaa0883` / `753bb32` | profile query boundary 与 durable CI evidence DONE |
| EXEC-008 | `2ecc662` | v0.3 contracts/schema/migration DONE |
| EXEC-009 | `67e3ef9` | deterministic B3 kernel DONE |
| EXEC-010 | `82bb5b1` | sequential/anti-oscillation DONE |
| EXEC-011 | `d95e0a4` | cross-system adaptive execution DONE |
| EXEC-012 | `71d05a2` | outcome/experiment/OPVE DONE |
| EXEC-013 | `feat(EXEC-013): complete v0.3 release gate` | 本地独立完成提交 |

Durable GitHub evidence：

- candidate：`eaa0883d40fce67648bddb112f7bc8be4ee0b1c8`；
- workflow：`Askora CI`；
- run：<https://github.com/Xike-yuhuofei/Askora/actions/runs/31182300958>；
- 复核结果：9 个 jobs 全部 `success`，包括 Python 3.11/3.12 backend tests、ruff/formatting/type baseline、Alembic、frontend build、前后端 dependency audit 与 private container build。

EXEC-008～013 未 push；最终实现使用与 CI 相同的锁定命令在本地重新执行。没有伪造当前本地 commit 的 GitHub status。

## 3. Implementation Summary

最终 gate 补齐并验证：

- 真实模型 gate 切换到 v0.3 canonical adaptive path；
- fixed TeachingContext/PolicyBundle 经 SYS05 决策后，由 SYS02/SYS08 在 envelope 内执行；
- actual assistance 进入 SYS04 deterministic AssessmentResult 与 SYS03 LearnerEvidence；
- 同一 SYS03 `WeightedBKTProjector` 可消费 v0.2/v0.3 evidence，不新增第二 mastery owner；
- v0.3 E2E 串联 MasteryEstimate、ReviewSchedule、next SYS05 decision 与 OutcomeObservation；
- 修改过的 projector 已从不可扩大的 Black legacy baseline 移除。

## 4. Canonical E2E / Observability Trace

自动化 E2E 覆盖并关联：

```text
LearningActivity / TeachingContext
→ exact PolicyBundle
→ SYS05 DecisionTrace + TeachingAction
→ SYS02 EvidenceBundle / RetrievalTrace
→ SYS08 rendered response + actual assistance
→ SYS04 Attempt + AssessmentResult
→ SYS03 LearnerEvidence + MasteryEstimate
→ SYS07 ReviewSchedule
→ next TeachingContext + SYS05 decision
→ TeachingEpisode + LearningTrajectory
→ OutcomeObservation
```

v0.2 E2E 继续验证真实 Markdown/SourceSpan、LearningGoal/Plan、SQLite、outbox/restart/replay；v0.3 E2E 在其上验证 adaptive decision/outcome 链。DecisionTrace 保持 decision-time 内容，OutcomeObservation 只记录 later measurement。

## 5. Scenario Variants

| Variant | Evidence | Result |
|---|---|---|
| V1 low/insufficient evidence | six-family scenario + low-confidence tests | conservative EXPLICIT/GUIDED path PASS |
| V2 assisted success | actual-assistance integration + real-model gate | discounted evidence + validation obligation PASS |
| V3 answer exposed | actual assistance eligibility | zero independent weight + obligation PASS |
| V4 fresh independent success | sequential/E2E | fade/retrieval eligibility PASS |
| V5 delayed retrieval | fixed-time v0.3 E2E + review scheduler | delayed evidence/review/outcome PASS |
| V6 transfer | six-family/G1 fixtures | TRANSFER_CHALLENGE + novelty contract PASS |
| V7 repeated failure | sequential replay | ceiling override/remediation PASS |
| V8 UNKNOWN/low confidence | kernel/sequential fixtures | conservative probe, no forced diagnosis PASS |
| V9 no material evidence | sequential replay | no illegal oscillation PASS |
| V10 model failure/invalid output | adaptive execution integration | tightening fallback, no learner failure PASS |
| V11 retrieval missing | adaptive retrieval integration | explicit missing/fail-tight PASS |
| V12 prompt injection/override | v0.2/v0.3 security suites | hard rule/tool/exposure override denied PASS |
| V13 restart pending work | recovery/outbox tests | retry or explicit terminal state PASS |
| V14 migration/replay | 9 fixtures + Alembic test | ambiguity explicit, no fabricated FULL replay PASS |
| V15 experiment probability | kernel/outcome tests | assignment probability separated from null propensity PASS |

## 6. Vertical Slice DoD Matrix

| # | Requirement | Evidence / Result |
|---:|---|---|
| 1 | v0.2 loop intact / single writer | v0.2 E2E + architecture suites PASS |
| 2 | profile read boundary | EXEC-007 architecture/integration tests PASS |
| 3 | durable CI evidence | `eaa0883`, run `31182300958`, all jobs success |
| 4 | v0.3 unique active writer schema | contract/ownership tests PASS |
| 5 | 9 migration candidates | exact 9 fixtures, idempotent PASS |
| 6 | six families / four layers | contract/unit/OPVE L1/L2 PASS |
| 7 | exact TeachingContext replay | kernel/replay tests PASS |
| 8 | immutable PolicyBundle | persistence/activation tests PASS |
| 9 | policy layers independently testable | unit/integration suites PASS |
| 10 | G0 100%, forbidden 0 | 4/4, forbidden=0 PASS |
| 11 | G1 acceptable set | 4/4 PASS |
| 12 | transition edge cases | sequential suite PASS |
| 13 | validation obligation | assisted/answer-exposed tests PASS |
| 14 | SYS02/SYS08 tightening-only | integration/security properties PASS |
| 15 | actual assistance consumed | v0.3 E2E + eligibility tests PASS |
| 16 | legacy Socratic no final owner | architecture regressions PASS |
| 17 | deterministic null propensity | contract/kernel tests PASS |
| 18 | DecisionTrace/Outcome split | immutable persistence/E2E PASS |
| 19 | no ninth truth owner | ownership architecture tests PASS |
| 20 | OPVE/replay/migration/recovery/security | required grouped suite PASS |
| 21 | real-model E2E | DeepSeek v0.3 canonical gate PASS |
| 22 | three release statuses auditable | this report + executable evidence PASS |
| 23 | no blocking SPEC GAP | none identified PASS |

## 7. EXEC-013 AC Matrix

| AC | Result | Evidence |
|---|---|---|
| AC-001 | PASS | v0.2 E2E + ownership tests |
| AC-002 | PASS | EXEC-007 boundary tests + durable CI run |
| AC-003 | PASS | v0.3 contracts/writer cutover + 9 fixtures |
| AC-004 | PASS | recovery/outbox/migration/replay suites |
| AC-005 | PASS | real configured DeepSeek gate, no secret persisted |
| AC-006 | PASS | durable CI jobs + current local equivalent tests/build/audits |
| AC-007 | PASS | six-family/four-layer contracts |
| AC-008 | PASS | exact input determinism tests |
| AC-009 | PASS | G0 4/4, forbidden=0 |
| AC-010 | PASS | G1 4/4 acceptable-set membership |
| AC-011 | PASS | hard constraints never restored by score/LLM/fallback |
| AC-012 | PASS | sequential transition suite |
| AC-013 | PASS | tightening-only + actual-assistance E2E |
| AC-014 | PASS | legacy Socratic ownership architecture test |
| AC-015 | PASS | deterministic/null propensity; assignment separate |
| AC-016 | PASS | DecisionTrace immutable; delayed association-only |
| AC-017 | PASS | four primary vs six process metrics disjoint |
| AC-018 | PASS | `LEARNING_EVIDENCE_INSUFFICIENT` retained |
| AC-019 | PASS | DoD 1～23 mapped above |
| AC-020 | PASS | no blocking SPEC GAP |

## 8. OPVE / Gold Sets

```text
L1 Contract Verification: PASS
L2 Scenario Replay: PASS (six StrategyFamily)
L3 Sequential Transition Replay: PASS
L4 Property / Metamorphic: PASS
L5 Baseline Differential: PASS (behavior only)
L6 Synthetic Stress: PASS (200 cases)

G0: 4/4 = 100%; forbidden action = 0
G1: 4/4 selected_action in acceptable_actions
G2: 2 research/calibration cases; not a hard release gate
Synthetic label: ENGINEERING/POLICY EVIDENCE ONLY
```

## 9. Real Model

非敏感证据：

```text
provider: deepseek
model: deepseek-chat
prompt_version: v03-policy-bound-render/1.0
policy_bundle_version: policy-1
result: success
latency_ms: 1404
actual_assistance: ASSISTED
next_strategy_family: GUIDED_PRACTICE
```

路径实际完成：fixed v0.3 action → EvidenceBundle → real model → SYS08 validation → actual assistance → deterministic AssessmentResult → SYS03 evidence → next SYS05 decision。密钥未提交、未进入报告或日志。

## 10. Migration / Replay

9 candidates：

- `historical_replay` → `NON_REPLAYABLE`（exact refs missing）；
- 其余 8 类 → `PARTIAL`，保留 canonical target、compatibility behavior、ambiguity reason 与 retirement condition；
- ambiguous legacy propensity → `action_propensity=null`、behavior unknown、PARTIAL；
- upcaster 两次执行 semantic output 相同；
- v0.2 representative DB 可前向升级、回退至 previous head、再升级；
- `alembic check` 无待生成操作；
- replay 不调用在线 LLM，不用当前 mutable state 补历史 refs。

## 11. Recovery

- pending outbox 在 process restart 后恢复为 durable retry；
- stale processing 不重复 canonical evidence/write；
- v0.2 restart/replay E2E 保持 deterministic；
- provider/invalid output/retrieval missing 进入结构化 failure 或 tightening fallback，不产生 learner failure evidence。

## 12. Security

固定回归通过：document/retrieval prompt injection、hard-rule override、grader/answer leakage、SYS02/SYS08 expansion、unauthorized tool、secret/log leakage、path traversal、unsupported citation、legacy Socratic ownership override。

## 13. Ownership

```text
Knowledge/relations             SYS01
EvidenceBundle/RetrievalTrace   SYS02
LearnerEvidence/MasteryEstimate SYS03
Attempt/AssessmentResult        SYS04
TeachingAction                  SYS05
LearningPlan/Activity           SYS06
ReviewSchedule/next_due         SYS07
Model/tool/workflow execution   SYS08
```

Outcome/Experiment 只作为 immutable analytics/control ledger records，不形成第九 truth owner。最终 TeachingAction owner 始终是 SYS05。

## 14. Tests

| Command | Result |
|---|---|
| `pytest tests/e2e tests/integration tests/security tests/recovery tests/migrations tests/evals tests/architecture` | 92 passed, 1 protected-marker skipped |
| `ASKORA_RUN_REAL_MODEL=1 pytest tests/evals/test_real_model_e2e.py` | 1 passed，真实 DeepSeek |
| `pytest` | 237 passed, 1 protected-marker skipped |
| `ruff check app tests scripts test_document_service.py test_optimizations.py` | PASS |
| `mypy app --no-error-summary` | PASS |
| `check_black_baseline.py` | PASS，201 files checked；baseline 减少 1 |
| direct `black --check ...` | 12 个既有、哈希锁定 legacy baseline 文件未格式化；本次文件均通过 |
| `alembic upgrade head && alembic check` | PASS，head `d6a1c3f90308` |
| `python test_document_service.py && python test_optimizations.py` | PASS |
| `npm ci && npm run build` | PASS |
| `npm audit --audit-level=high` | 0 vulnerabilities |
| `pip-audit --progress-spinner=off` | no known vulnerabilities |

## 15. Known Legacy Debt

- Black direct gate 仍有 12 个 EXEC-007 之前的精确哈希 baseline 文件；正式 CI 会阻止范围扩大。本次修改的 `projector.py` 已格式化并移出 baseline。
- legacy 目录仍保留 bounded adapters/execution components；architecture tests 阻止它们取得 canonical ownership。
- 当前最终提交只做本地 commit，未 push；因此不声称它已有新的 GitHub status check。Durable CI 证据来自 EXEC-007 candidate，最终实现另有完整本地等价门禁。

## 16. SPEC GAP

```text
SPEC GAP: none
```

## 17. Final Status

```text
Status: DONE
Engineering Gate: PASS
Policy Correctness Gate: PASS
Learning Evidence Gate: LEARNING_EVIDENCE_INSUFFICIENT
```

v0.3 Adaptive Teaching Loop engineering/policy vertical slice frozen。
