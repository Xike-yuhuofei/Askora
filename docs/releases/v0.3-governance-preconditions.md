# Askora v0.3 — Governance Preconditions Evidence

> EXEC：`EXEC-007`
> Gate status：`PASS`
> Candidate commit：`eaa0883d40fce67648bddb112f7bc8be4ee0b1c8`
> Workflow：`Askora CI`
> Workflow run：<https://github.com/Xike-yuhuofei/Askora/actions/runs/31182300958>
> Recorded at：2026-08-07

## 1. Profile query boundary

- `GET /users/profile` 只依赖 `app.queries.profile.ProfileQueryService`，不再 import/query `UserProfile` ORM。
- canonical mastery 从 SYS03 `MasteryEstimateRecord` latest-version read projection 读取。
- canonical response 只投影 `competence_probability`、confidence、evidence counts、misconception refs 与 algorithm version；不使用单一概率阈值自行声明 stable mastery。
- `skills_mastered`、`mastery_summary` 等既有字段仅作为显式 `legacy_compatibility` projection 保留，以维持前端核心字段兼容；它们不覆盖 canonical projection。
- Architecture regression 禁止 API 直接 import legacy profile ORM、canonical mastery persistence model 或 learner repository implementation。

Compatibility retirement condition：学习聚合字段在所有受支持前端消费者改读 SYS03 projection 后退役；偏好/统计字段只在存在明确 canonical owner query 且前端迁移完成后退役。

## 2. Durable GitHub checks

| Check | Result |
|---|---|
| `Backend tests / Python 3.11` | PASS |
| `Backend tests / Python 3.12` | PASS |
| `Ruff, formatting and type baseline / Python 3.11` | PASS |
| `Ruff, formatting and type baseline / Python 3.12` | PASS |
| `Alembic migration validation` | PASS |
| `Frontend build` | PASS |
| `Frontend dependency audit` | PASS |
| `Python dependency audit` | PASS |
| `Private container build` | PASS |

Black 的既有未格式化文件通过内容 SHA-256 锁定为不可扩大的 baseline；任何 baseline 文件内容变化都会失败，新增及其他 Python 文件继续执行 Black 检查。

## 3. Gate conclusion

- `EXEC007-AC-001..008`：PASS。
- Blocking `SPEC GAP`：none。
- External CI blocker：none。
- 本证据仅关闭 EXEC-007 governance preconditions，不包含或启动 EXEC-008 及后续实现。
