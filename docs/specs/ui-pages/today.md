# 「今天」页面（`/today`，**退役/迁移页**）

> **页面职责**：**无**。被冻结的空间中心 IA（`EXP-IA-001`）已移除「今天」L0 心智，由「欢迎」与空间列表取代；本页仅作路由兼容跳板。
> **对应契约**：`EXP-IA-001`（「今天」不再是 L0）、`EXP-JOURNEY-001`（回来继续的入口是空间）、`UI-ROUTE-002`（兼容跳板）
> **现状基准**：`apps/frontend/src/pages/Today.jsx`（legacy「今天」landing；H1「今天」冲突，见 §6）

---

## 1. 处置决定

1. 页面 H1「今天」违反冻结 IA（L0 应为「欢迎」）。**不再作为 landing/首页**。
2. 历史 deep-link `/today` 保留为**兼容跳板**：重定向到「欢迎」（`/`），不渲染旧界面。
3. 若过渡期需展示兼容内容，仅允许作为一次性的「欢迎」说明卡，文案使用「欢迎」字眼，不出现「今天」标题。

## 2. 现有元素（迁移清单）

| # | 现有元素 | 文案 | 处置 |
|---|---|---|---|
| TD-01 | 主标题 | H1「今天」 | 删除；由「欢迎」承接（`welcome.md`） |
| TD-02 | 兼容学习入口 | 「快速学习」「开始兼容学习」 | 迁移到「欢迎」/空间内的「继续学习」（learning-workspace.md） |
| TD-03 | 目标选择 | 「选择目标」 | 迁移到学习路径页的目标 select（learning-path.md LP-03） |
| TD-04 | 快速任务 | 「检查目标」「安排验证」 | 迁移到目标详情（goal-detail.md）上下文入口 |

## 3. 路由兼容跳板

| 原路由 | 跳转目标 | 说明 |
|---|---|---|
| `/today` | `/`（欢迎） | 直接重定向；无 UI |

## 4. 禁止事项

- 恢复「今天」为 L0 或 landing（`EXP-IA-001`）。
- 空态/文案继续引用「今天」时间心智（已冻结，见 history.md §6 同类处理）。
