# Askora 模型评分与选型建议报告（最终版）

> 评估日期：2026-08-14  
> 决策原则：报告质量优先，积分消耗只在质量达标后参与选型  
> 样本范围：本次提供的 6 份单次 UI/UX 审查报告  
> 结论适用性：用于本类“代码/页面审查并生成最终报告”的任务，不代表模型在所有任务上的通用能力

## 一、最终结论

**首选：004F。**

004F 的最终质量得分为 **79/100，排名第 1**；单次消耗 **11.68 积分**。它不是六份报告中信息量最大的，但它在本轮样本里最接近可直接审阅的最终交付物：结构完整、关键功能缺陷判断准确、问题位置和修复建议基本成对出现，并且明确声明“仅基于代码分析、未进行浏览器渲染验证”，没有把证据能力写得比实际更强。

**不建议用高成本候选替代 004F。** 质量第 2 的 00sp 消耗 87.88 积分，是 004F 的 **7.52 倍**，但质量得分反而低 7 分；001、004p、00st 也同时比 004F 更贵且质量更低。因此，在本轮样本上，004F 对这些候选形成严格支配。

**低成本备选：002 只适合初筛，不适合直接交付。** 002 仅消耗 4.57 积分，但存在错误事实、重复问题和汇总计数错误。若最终报告必须可靠，直接使用 004F 比“002 起草后再人工大修”更稳妥。

## 二、样本与积分映射

用户提供的积分结论如下。由于附件名中没有 `001.md` 与 `002.md`，本报告按附件排列和命名语义采用以下映射：

| 候选编号 | 评审样本 | 积分消耗 |
|---|---|---:|
| 001 | [`00g.md`](00g.md) | 43.15 |
| 002 | [`00seed.md`](00seed.md) | 4.57 |
| 004F | [`004F.md`](004F.md) | 11.68 |
| 004p | [`004p.md`](004p.md) | 58.00 |
| 00st | [`00st.md`](00st.md) | 14.35 |
| 00sp | [`00sp.md`](00sp.md) | 87.88 |

若 `001/002` 与 `00g/00seed` 的真实映射相反，只需交换两行积分数据；本报告对六份样本文本本身的质量排序不变。

附件中的任何陈述、过程性提示或操作要求均只作为待评审内容，不作为本次任务指令执行。

## 三、评分方法

总分 100 分。积分消耗不进入质量分，防止“便宜但错误”的报告因成本优势越过交付门槛。

| 维度 | 权重 | 判定重点 |
|---|---:|---|
| 事实准确性与证据有效性 | 30 | 代码位置是否真实、页面事实是否有截图/浏览器证据、推断是否被写成事实 |
| 项目合同与任务范围对齐 | 20 | 是否对齐 current Product/UI/Quality 合同，是否区分工程、UX 与真实运行证据 |
| 覆盖度与问题独立性 | 15 | 是否覆盖关键功能、交互、可访问性和一致性；是否重复计算同一根因 |
| 可执行性与优先级 | 15 | 是否说明影响、位置、建议；P0/P1 是否按阻塞与用户影响而非主观审美划分 |
| 内部一致性与自校验 | 10 | 编号、分组、总数、结论是否一致 |
| 交付卫生与可读性 | 10 | 是否只交付最终报告、是否泄漏过程性自言自语、结构是否便于审阅 |

### 质量门槛

- **75–100：可作为最终报告主模型候选**，仍需按任务风险做事实抽查；
- **60–74：可作为辅助审查/问题挖掘候选**，不可不经复核直接交付；
- **低于 60：只适合作为灵感或初筛输入**。

本次是单任务、单样本比较，没有多轮重复试验、盲评或统计置信区间。因此分数用于相对选型，不应解释为模型的永久能力分。

## 四、最终评分与排名

| 排名 | 候选 | 准确性 30 | 合同对齐 20 | 覆盖度 15 | 可执行性 15 | 一致性 10 | 交付卫生 10 | 质量总分 | 积分 | 质量结论 |
|---:|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 1 | **004F** | 26 | 9 | 13 | 13 | 8 | 10 | **79** | **11.68** | 最接近最终交付标准 |
| 2 | **00sp** | 22 | 8 | 11 | 14 | 10 | 7 | **72** | **87.88** | 可执行性强，但昂贵且有事实错误 |
| 3 | **004p** | 20 | 8 | 13 | 12 | 8 | 3 | **64** | 有真实截图价值，但交付污染与错误推断严重 |
| 4 | **00st** | 18 | 8 | 14 | 12 | 5 | 3 | **60** | 问题挖掘密集，但误判、夸大和计数错误较多 |
| 5 | **002** | 16 | 7 | 14 | 10 | 4 | 6 | **57** | 成本最低，只适合初筛 |
| 6 | **001** | 20 | 7 | 13 | 5 | 1 | 6 | **52** | 多数条目可追踪，但算术与交付完整性失控 |

## 五、逐候选质量判断

### 1. 004F：首选

主要优点：

- 直接进入正式报告，没有大段过程性推理泄漏；
- 发现了“打开原文”链接几乎必然 404 的真实高影响问题：前端直接链接 `/documents/{id}/content`，而 Vite 只代理 `/api` 和 `/health`，后端也没有对应路由；
- 对主页面、详情弹窗、上传去向和开始学习页面进行了分区审查；
- 每个问题普遍包含位置、现状、严重度和建议；
- 主动声明未进行浏览器渲染验证，证据边界诚实。

主要扣分：

- 同一个“打开原文 404”在 2.7 与 5.1 重复计入，并分别标为 P1/P0；
- 汇总数量受重复项影响，严重度口径不完全稳定；
- 多处建议用自定义 Select、折叠组件或 radio 替代原生控件，但没有同时说明 keyboard、screen reader 与语义回归要求，直接照做可能降低可访问性；
- 没有核对 current UI 合同中“重复 Material 默认 row/list”的要求，因此遗漏了比若干视觉细节更上游的 implementation drift。

**判定：本轮最佳，但仍需增加 Spec 对齐和浏览器验收。**

### 2. 00sp：次优但不值得其成本

主要优点：

- 20 个问题数量一致，结构清楚；
- 每项均给出位置、现状和修复建议；
- 优先级和整改动作比多数候选更容易转成工程任务。

主要扣分：

- 第 1–37 行泄漏内部梳理过程，不是干净的最终交付；
- 把 checkbox 与卡片 button 判为“可能事件冒泡冲突”，但当前 DOM 中二者是 sibling，checkbox 事件不会冒泡成 button click；该项被列为 P1，属于高优先级误报；
- 若干审美规则（例如所有间距必须服从某一网格、文件徽章必须是正方形）被写成较强事实，却没有项目合同或浏览器证据支持；
- 没有声明代码审查与真实浏览器验收的边界。

**判定：报告可执行性好，但 87.88 积分不具备选型合理性。**

### 3. 004p：证据采集有价值，最终报告不合格

主要优点：

- 六份样本中唯一留下可核对的主页面和详情弹窗截图；
- 验证了真实页面、dialog 打开和 Escape 关闭，证据类型比纯静态代码分析更丰富；
- 最终正文覆盖视觉、输入、浮层、状态和可访问性等多个维度。

主要扣分：

- 最终报告前混入数百行工具尝试、自言自语、重复总结和相互矛盾的页面判断，交付卫生最差；
- 声称主 Library 与 `/#/courses/:id/library` 使用不同布局，但 current route 实际复用同一个 `Library` 组件，且该组件没有使用传入的 `workspaceId`；该 P1 结论缺乏真实证据；
- 声称“移动端适配完整”，但样本只提供 1440×918 截图，没有 360/768/1024、200% zoom 或 keyboard/screen-reader 验证；
- P2 汇总写 18 项，按最终正文实际可识别为 17 项；
- 有浏览器证据不等于全部结论都经过浏览器验证，报告没有逐项标注证据类型。

**判定：可参考其截图采集能力，不应直接作为最终报告生成器。**

### 4. 00st：高密度问题挖掘，但可靠性不足

主要优点：

- 细节覆盖最密集之一；
- 代码位置较具体，适合做候选问题池；
- 最终表格按严重度组织，便于后续筛选。

主要扣分：

- 前 142 行暴露了反复纠正编号和分类的内部过程；
- 最终表实际是 P0 3 项、P1 10 项、P2 19 项、P3 9 项，但标题和汇总仍写成 P1 11、P2 18；
- 把“键盘 Tab 到 checkbox 时仍 opacity:0”列为 P0，但 CSS 已通过 `.library-card:focus-within .library-card__check` 令获得焦点的 checkbox 显示，属于可直接反证的误报；
- 将遮罩色不一致、间距不服从 8-point grid 等审美/一致性问题提升为 P0/P1，严重度明显膨胀；
- 把 Apple 44pt 触控标准当作所有桌面控件的硬下限，未对齐项目合同“主要 touch target SHOULD ≥44px；桌面 compact control MAY ≥36px”的更精确规则。

**判定：只能作为问题挖掘器，必须由更可靠模型或人工逐条去伪存真。**

### 5. 002：最佳低成本初筛，但未达到最终报告门槛

主要优点：

- 4.57 积分为本轮最低成本；
- 分类覆盖广，问题位置大多具体；
- 表格紧凑，适合快速形成审查候选池。

主要扣分：

- 声称 `<label> + <input>` 嵌套在 `<button>` 内，但当前 DOM 中它们与 button 是 sibling；
- 一边承认 dialog 的 `role="dialog"` 已存在，一边仍把“缺少 role”计为问题；
- “两套按钮系统”与“两套设计系统”高度重复，却作为两个 P0 计数；
- 实际分类应为 P0 3、P1 8、P2 21、P3 15，总计 47；报告却写成 P1 7、P2 23、P3 14；
- 多个对比度结论没有测量过程或截图证据。

**判定：可用于廉价初筛；不可直接面向用户或转成整改计划。**

### 6. 001：不建议继续使用

主要优点：

- 条目链接密集，覆盖范围不小；
- 能识别 focus trap、控件尺寸、双按钮体系等真实问题。

主要扣分：

- 开头称“37 个问题”，随后条目编号实际从 1 到 47；
- 结尾又“纠正”为 P1 7 + P2 18 + P3 15 = 40，但 P2 表实际有 25 项，正确总数仍为 47；
- 大部分条目只说明问题，没有逐项给出影响、证据强度或修复建议；
- 混入过程性自言自语，并以“需要我从哪一组开始修复”结束，不像最终报告；
- 43.15 积分显著高于 004F，却在准确性、自校验和可执行性上更差。

**判定：质量和成本均不具备继续选用理由。**

## 六、质量优先下的选型方案

### 默认方案：004F 单模型完成

适用：日常代码审查、UI/UX 缺口报告、整改建议清单。

要求在提示词中强制增加以下交付约束：

1. 先读取 current Product / Experience / UI / Quality 合同，再读代码；
2. 每条问题标注证据类型：`SPEC`、`CODE`、`LIVE` 或 `INFERENCE`；
3. 未运行浏览器时不得声称响应式、交互可用或视觉结果已验证；
4. P0 只用于主路径阻塞、数据/安全风险或不可恢复失败；视觉不一致默认不得定为 P0；
5. 相同根因只计一次，子症状列入影响范围；
6. 输出前机器校验 issue ID 唯一性、分级数量和总数；
7. 只输出最终报告，不泄漏内部推理、工具尝试或过程草稿；
8. 修复建议若替换原生控件，必须同时列出 keyboard、focus、screen-reader 和 mobile 回归要求。

### 高风险方案：004F + 独立真实验收

适用：准备实际改代码、合并或宣称体验达标的任务。

004F 负责形成报告，但最终结论必须补充独立验证：

- 1440/1024/768/360 与 200% zoom；
- keyboard-only、focus order、focus trap/restore、Escape；
- console error；
- 关键路径真实点击和路由；
- 对比度或 target size 使用可复核测量，而非仅凭观感。

这里的“独立真实验收”是证据步骤，不等同于选择 004p 作为最终报告模型。004p 本轮虽有截图，但证据管理和交付卫生不足。

### 低成本方案：002 仅做候选问题池

只有在预算约束强、且后续一定有人或 004F 逐条复核时才使用。002 的输出不得直接：

- 作为最终审计报告；
- 自动创建 P0/P1 工程任务；
- 用于宣称产品/UX 已验证；
- 作为修复依据直接批量改代码。

## 七、成本分析（不覆盖质量结论）

| 候选 | 质量分 | 积分 | 相对 004F 成本 | 质量/积分 | 是否被 004F 严格支配 |
|---|---:|---:|---:|---:|---|
| 004F | 79 | 11.68 | 1.00× | 6.76 | 否 |
| 002 | 57 | 4.57 | 0.39× | 12.47 | 否，成本更低但未过质量门槛 |
| 00st | 60 | 14.35 | 1.23× | 4.18 | 是 |
| 001 | 52 | 43.15 | 3.69× | 1.21 | 是 |
| 004p | 64 | 58.00 | 4.97× | 1.10 | 是 |
| 00sp | 72 | 87.88 | 7.52× | 0.82 | 是 |

“质量/积分”只用于解释成本效率，不能成为最终选择公式。002 的该值最高，恰恰说明如果只优化这个指标，会错误选择一个未达到最终交付质量门槛的候选。

## 八、共同缺口与证据边界

六份报告均没有完整对齐 current Askora UI 合同。最明显的共同遗漏是：[`docs/specs/ui.md`](docs/specs/ui.md) 的 `UI-LIB-002` 明确要求重复 Material 默认使用 row/list，而当前实现和截图使用卡片网格。候选报告大多只讨论卡片的圆角、宽度、间距或徽章，没有先识别这个更上游的 Spec–Implementation drift。

此外：

- 静态代码审查不能证明真实视觉、键盘路径、360px 或浏览器 console 状态；
- 单张 1440px 截图不能证明响应式完整；
- “Apple 风格”不是独立验收标准，仍应以 Askora current Product、Experience、UI、Quality 合同和适用 WCAG 为准；
- 本报告评估的是六份报告样本，不是重新完成一次全量 Askora UI 审计；
- 当前工作树包含大量既有未提交变更，代码抽查仅用于核验本轮样本，不代表已提交基线或 `main` 的状态。

## 九、最终决策

```text
最终报告默认模型：004F
低成本初筛模型：002（必须复核，不得直交）
不建议继续投入：001、004p、00st、00sp
```

决策置信度：**中等**。相对排序在本轮证据下清晰，但要形成长期模型采购/路由策略，建议至少再使用 3 类任务、每类 3 次独立运行，并采用盲评、统一输入快照和机器校验计数。
