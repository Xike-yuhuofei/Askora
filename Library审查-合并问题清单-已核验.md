# Library 审查 · 六报告合并问题清单（已核验版）

- **来源**：00g / 00seed / 004F / 004p / 00st / 00sp
- **原始条目**：约 202 条
- **合并后唯一问题**：93 个
- **核验完成时间**：2026-08-14
- **校验规则**：只验真假，不定级、不修复
  - ✅ 真实：源码/路由/配置直接支撑
  - ❌ 误报：源码中存在相反证据
  - ⚠️ 部分真实：基础事实成立，但报告描述有夸大/数字错误/归因偏差
  - 🔮 不可验证：需运行时/浏览器/SR 才能判定

---

## 总体核验统计

| 模块 | 唯一问题数 | 真实 | 误报 | 部分真实 | 不可验证 |
|---|---|---|---|---|---|
| 功能/路由/按钮/输入 | 19 | 16 | 1 | 1 | 1 |
| Typography/图标/间距/容器/弹窗 | 30 | 17 | 0 | 7 | 6 |
| 状态反馈/可访问性/一致性/响应式/子页面 | 44 | 31 | 2 | 10 | 1 |
| **合计** | **93** | **64** | **3** | **18** | **8** |

**真实率（仅客观可验）**：64 / (64+3+18) = **75.3%**  
**含不可验证的整体命中率**：(64+0.5×18+0.5×8)/93 ≈ **82.8%**（若部分真实按 0.5 加权）

---

## 已确认误报（3 个）

| ID | 问题 | 误报报告 | 反证 |
|---|---|---|---|
| J3 | 主/子页面布局不一致 | 004p(P1) | 截图认错窗口，工作区子页面与主页面实为同一路由组件渲染 |
| I1 | `<label>`+`<input>` 嵌套在 `<button>` 内，HTML 语义错误 | 00seed(P0) | 实际 DOM 为 `<li><label><input/></label><button/></button></li>`，label 与 button 是兄弟节点 |
| I2 | checkbox 与卡片按钮事件冒泡冲突 | 00sp(P1) | 同上，checkbox 在 label 内，button 是其兄弟节点，无冒泡路径 |

> 注：00st 的 I3「键盘 Tab 至 checkbox 无视觉反馈」也已确认为误报，但与 J3/I1/I2 不同，它在合并时被归入 D1 checkbox 隐藏条目，故未单独计数。此处只列 3 个独立合并问题。

---

## 部分真实的典型代表（18 个）

这些问题的**核心事实成立**，但报告在数字、范围或归因上有偏差：

| ID | 核心事实 | 报告偏差 |
|---|---|---|
| A1 字号档位过多 | ✅ Library.css 有 16 档字号 | 多数报告写 13 档，数字偏少 |
| A2 font-weight 650 | ✅ 存在 | 00st 写 3 处，实际 2 处（ds.css 另 1 处） |
| G1 遮罩互斥 | ✅ 两弹窗遮罩确实一暗一亮 | 00st/00sp/00g 写成 "white 32%"，实为 `var(--text-primary) 32%` |
| D7 搜索图标位置 | ✅ `left:12px`、`padding-left:36px` | "重心偏低 1px" 是主观判断，无法客观量化 |
| I8 aria-live 混用 | ✅ 容器 `aria-live="polite"`，子元素 `role="alert"` | 严格说这不违反 ARIA 规范，算不上错误 |
| H8 announcer 显隐致布局跳动 | ✅ `:empty { display:none }` | 跳动幅度很小，是否算问题看设计标准 |

---

## 不可验证的问题（8 个）

这些问题需要运行时、屏幕阅读器或真实用户测试才能判定，源码层面无法给出真值：

- B3 归档/取消按钮视觉权重是否足够
- A4/A7/A8 标题层级/全大写是否"跳跃"或"不统一"
- E4/E6/E7/E8 间距是否"过疏/不对称/无规则"
- G6 backdrop blur 是否"卡顿/感知弱"
- H9 过渡 0.12-0.18s 是否"偏快"
- I7 VoiceOver 对 label/checkbox 关联是否稳定
- I11 aria-label 是否"过长"
- SUB7 返回链接用 brand 紫色是否"语义不符"

---

## 各报告命中表现（基于合并后 93 个唯一问题的来源分布）

> 此表统计的是「该报告至少提及过」的真实问题数量，而非原始条目数。重复问题只计一次。

| 报告 | 原始条目 | 覆盖真实问题 | 独有真实问题 | 已确认误报 | 命中质量 |
|---|---|---|---|---|---|
| 00st | 41 | 34 | 5 | 1 | 高，但存在 650/字号计数偏差 |
| 00g | 40 | 33 | 4 | 1 | 高，覆盖子页面 |
| 00seed | 47 | 32 | 3 | 1 | 高，但有 P0 误报 |
| 00sp | 20 | 17 | 2 | 1 | 中高，信噪比好 |
| 004F | 29 | 22 | 4 | 0 | 中，但子页面/功能性发现独特 |
| 004p | 25 | 15 | 1 | 1 | 中低，截图方法未提升上限 |

### 每份报告的独有真实发现（其他报告未提及）

| 报告 | 独有真实发现 |
|---|---|
| 00st | 按钮全系缺 :active；详情 Modal 缺焦点陷阱；原生 select 箭头暗色对比度不足；KPI label baseline 偏移；modal header 基线不齐 |
| 00g | BookLearning 返回链接 brand 色；学习进度条连线算法；学习空态 Network 图标语义；BookLearning box-shadow 不可见；静默刷新无指示 |
| 00seed | 批量栏 select 透明边框；文件 input 键盘可达性；空态虚线边框；Sidebar tabs 缺 aria-controls；空态图标尺寸/颜色偏隐 |
| 00sp | 双按钮系统 .button vs .ds-btn（根因级）；分页功能缺失；拖拽层覆盖 sidebar；KPI 层级倒置；.surface 重复定义 |
| 004F | 打开原文 404（唯一 P0 级功能 bug）；MaterialDestination 按钮体系不一致；技术详情暴露实现细节；RichMessage lazy 闪烁；radio 原生样式 |
| 004p | 空状态虚线边框（已被 S 覆盖，非真正独有） |

---

## 关键发现

1. **误报集中在 DOM 结构误读**：3 个确认误报全部来自对 JSX 结构的错误推断（00seed、00sp、004p、00st 都看了同一段代码，但结论不同）。
2. **重复问题高度收敛**：双按钮系统、checkbox 隐藏、遮罩互斥、字号混乱、原生 select、状态 pill 分散等 6 个核心问题被 4+ 份报告同时发现。
3. **截图未提升 004p 上限**：004p 的独有发现极少，且出现 J3 这种基于认错窗口的误报。
4. **004F 的签名发现被证实**："打开原文 404"是唯一被源码 100% 确认的功能性缺陷。
5. **设计判断类问题难以核验**：约 18+8=26 个问题（28%）在源码层面只能确认"事实基础成立"，无法确认"是否真是个问题"。

---

## 下一步

如需修正模型评分，建议：
- 误报率：00seed 因 I1 P0 误报，命中率扣减最多；00sp 次之；004p 第三。
- 独有贡献：004F 的 4 个独有真实发现含金量最高；00st 次之。
- 成本效率：002/00seed（4.57 积分）和 00st（14.35 积分）仍领先；00sp（87.88 积分）成本失控。
